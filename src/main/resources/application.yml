spring:
  ai:
    ollama:
      base-url: http://localhost:11434
      chat:
        options:
          model: llama3.2
          temperature: 0.7
          format: json  # Force JSON format for tool responses
  main:
    allow-bean-definition-overriding: true

logging:
  level:
    fr.eletutour.virtualmj: DEBUG
    org.springframework.ai: DEBUG